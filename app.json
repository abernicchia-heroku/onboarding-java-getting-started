{
  "name": "Start on Heroku: Java",
  "description": "A barebones Java app, which can easily be deployed to Heroku.",
  "addons": [ "heroku-postgresql" ]
	{
	  "environments": {
	    "test": {
	      "scripts": {
	        "test-setup": "echo "fake test-setup" && exit 0",
	        "test": "echo "fake test" && exit 0"
	      }
	    }
	  }
	}
}
